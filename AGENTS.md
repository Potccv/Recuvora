# Recuvora 开发规范

本文件面向贡献者和编码 Agent。项目导航见 [README](README.md)，运行设计见 [架构文档](docs/architecture.md)，组合、加载和生命周期见 [插件设计](docs/plugins.md)。

## 目录与文档

- 本目录为项目根；采用单个 Cargo 包 `recuvora`，源码按能力归入 `src/`，不建立 `packages/` 或专门收纳扩展的 `plugins/` 目录。
- 单包约束仅适用于宿主源码。节点及第三方宿主侧插件是独立项目，可独立选择语言、构建和发布；本项目不提供节点程序、节点发行包或节点编译角色，不要求外部项目依赖整个 `recuvora` crate。
- 使用标准 `src/lib.rs`、`src/main.rs`；库导出已实现的 `framework`、`recovery`、`monitors`、`boot`、`harnesses`、`interfaces`、`actions`、`nodes` 和 `protocol`，`server` 按 feature 编译。程序入口保持轻量，调用启动模块。能力分组通过模块和服务契约维护职责边界，不建立能力子包。
- Web 与 Desktop 前端共用 `src/interfaces/ui/public/` 中唯一一份静态 UI 源；Web 构建复制这组资产，Desktop 由同一 Cargo 包中的 Tauri 薄壳嵌入这组资产。不得为两个载体复制页面、业务视图、演示数据或样式。
- 官方 UI 包含完整标准业务页面：Harness 会话、修复详情、审批、日志查询、模拟实验室与通用管理均由本项目统一维护，不拆为需单独安装的插件页面。后台能力可来自内置服务、业务插件或节点；页面通过统一 HTTP 服务契约消费，第三方 UI 扩展仅补充官方尚未覆盖的专属视图。
- 前端选择由根 manifest 的 feature、显式二进制目标及 `scripts/build-ui.ps1` 的 `Web`、`Desktop`、`All` 目标表达，不建立第二个 Cargo 包、前端 workspace 或 `src-tauri/` 子包。Cargo feature 只控制编译与装配，不能充当业务授权或运行时能力开关。
- 项目根 `Cargo.toml` 统一登记包信息、依赖与测试目标，不使用 Cargo workspace。仅为实际实现声明模块，不创建空 manifest、伪启动入口或不可用配置。
- 测试代码统一位于 `tests/`，按模块与场景组织并在根 manifest 显式登记测试目标；故障注入通过集成测试自身的子进程入口运行，不单独建立测试支持包或产品可执行程序。需要访问私有模块的测试保持测试构建内引用，不为测试扩大产品 API。
- 测试生成的临时文件必须写入执行环境指定、且位于本项目根之外的统一临时目录。测试通过后立即清除不再需要的本次文件；失败排查结束后也须清除保留文件，且清理范围只能包含本次测试创建并记录的路径。
- `profiles/` 保存可审查的部署组合模板；`docs/` 保存共同设计；脚本有实际需求时再放入 `scripts/`。
- 根、`src/`、能力分组、平台子目录、`profiles/`、`docs/`、`tests/` 与 `scripts/` 维护 README.md 和 AGENTS.md。前者说明职责、契约、配置、限制与状态，后者补充开发约束；修改前阅读沿途适用规则。
- 局部规范继承上级约束，不削弱授权、持久恢复与运行时禁止自修改的边界；共同规则通过相对链接引用。
- 项目文档只含项目内容，不加入外层工作区布局、本地绝对路径或对话历史。设计、实现与已验证范围必须分开描述。
- 宿主源码、文档、示例和测试说明必须保持目标与供应商中立，只使用抽象 target/workload、provider observation 和 external action 语义；不得记录具体受监控产品、对象类别、实例名、业务字段、故障样本、活动部署或跨项目实机验收。此类资料由宿主项目之外的目标插件、节点或工作区集成记录维护。
- 项目目录只保留计划提交并推送到 GitHub 的源码、项目文档和必要版本控制配置；不得存放测试临时文件、构建产物、缓存、日志、录屏、运行数据或其他仅供本地使用的文件。
- UI 静态输出、资产清单、Cargo target、桌面可执行文件和安装产物必须写到项目根之外；构建脚本不得在源码 UI 目录生成 `dist`、缓存、哈希文件或运行数据。
- Tauri 辅助生成的 ACL/schema 同样放在项目外 Cargo `OUT_DIR` 的构建暂存目录；不得只用 `.gitignore` 掩盖源码内的 `gen/`。共享 UI 与原始配置只维护一份，新增 Tauri 输入同时更新暂存和变更跟踪。

## 框架、业务与装配

- `src/framework/` 只负责通用上下文、服务注册、依赖管理、事件、插件实例、生命周期及运行后端接口，不包含具体自愈流程、目标专属规则或模型提供方私有业务。
- `src/recovery/` 维护任务、授权、权威记录、调度及默认自愈流程；可信实现通过服务契约注册，默认随 Rust 宿主共同发布。
- `src/monitors/` 维护通用只读轮询、规则与时效/覆盖判定；具体采集属于外部节点或契约插件。检查点及故障变化交由 `recovery::incidents` 原子持久化；确认收到不解除故障，监控输出不产生授权或自动派发修复。
- `src/boot/` 负责宿主启动和组合校验；`src/nodes/` 负责宿主侧节点身份、连接、能力发现、路由及执行事实核对。节点本机采集、执行、持久记录和进程监督由外部节点项目实现；同机节点仍为独立程序。
- Desktop 启动代码只负责创建 Tauri 窗口并加载共享静态资产；Web 产物只是可由部署方托管的静态文件。两者都不得在前端复制 recovery 状态机、授权裁决、持久记录或 Harness 提供方实现。
- 内置领域契约、提供方及相关消费者放在各能力分组；第三方新增领域由独立插件维护。业务消费者依赖服务契约，不导入具体提供方内部实现。装配层可以显式选择提供方。
- `src/protocol/` 承载跨进程、跨节点的协议与代理映射；本地服务调用不强制序列化。SDK 复用框架公开接口和协议，不建立第二套权威规则。
- 新领域的命名空间、版本化契约/schema 及消费、流程、验证逻辑可由第三方宿主侧插件提供；通用协议与权限机制能够表达时，不要求为每个新业务修改或重新编译宿主核心。宿主侧指逻辑职责，独立安装插件默认进程外。契约注册不是执行授权，插件不得覆盖保留契约或绕过可信授权与权威记录服务；缺少插件或契约不兼容时停用相关能力。细则见 [插件设计](docs/plugins.md)。
- 插件元数据、配置、依赖及能力注册共同决定装配；源码模块、外部插件包、运行实例、工作进程和任务分别标识。不能把每个目录都视为插件或每项服务都拆成进程。
- 同一作用域内的权威服务提供者必须明确，避免双重授权、双重调度或恢复日志分叉。

## 语言与运行边界

- 可信宿主及内部关键业务优先安全 Rust；不为分层额外引入 Go。Python 用于确有依赖需要的视觉或外部能力，关键任务恢复不依赖 Python 进程存活。
- 内置模块与宿主共同编译和发布，可按配置启用、停用或选择已编入的实现；新增模块代码须更新宿主，不能称为可任意独立安装的动态插件。
- 独立安装或第三方扩展默认进程外。原生桌面、输入、视觉推理及复杂外部运行时以进程隔离保护宿主；简单可信监控、知识或通知实现可按评估在宿主内运行。
- 按信任、用户会话、资源和故障范围划分工作进程；多个可信能力可共享工作进程，但需说明共同失效范围。
- 本地与远程提供方遵守共同能力语义；远程代理必须显式处理延迟、超时、取消、断连及未知结果，不假装跨进程调用与本地调用没有差异。
- 首版不将任意 Rust 原生动态库加载作为通用第三方扩展机制，也不对其他第三方包或 API 提供隐式兼容承诺。
- 进程隔离不是权限沙箱；同进程模块属于宿主信任边界，框架接口不能约束任意恶意本机代码。用运行身份、文件权限及所选平台隔离机制落实外部执行范围。

## 生命周期与可靠性

- 服务声明版本及必需依赖；依赖缺失时等待或停用受影响实例，依赖变化触发可控的重新绑定。必需授权或持久服务缺失时不得继续自动执行。
- 事件区分瞬时通知和持久事实。订阅、服务注册、定时器及资源清理归属具体实例；effect/dispose 只管理这些资源，不自动撤销外部业务副作用。
- 卸载先停止新派发、处理在途调用，再撤销注册和释放资源。桌面与目标互斥锁只有在旧执行者无法继续操作后才能交还。
- 配置变更、实例重启、独立插件升级与宿主升级分别设计。关键服务默认使用受控停机、排空与恢复，不承诺任意热替换。
- 对消息、输出、缓存、日志、队列、并发和重试设限。隔离进程的监督必须覆盖子进程回收、超时、资源耗尽和有限退避。
- 使用宿主系统服务监督恢复 Recuvora 自身退出；不能依赖已经退出的进程自救。插件失效不能被报告成目标健康。
- 对可预期错误使用结构化结果；关键路径不随意使用 unwrap、expect 或 panic。unsafe/FFI 明确所有权、线程和资源生命周期；不能靠捕获 panic 代替故障隔离。
- 长期稳定性靠故障注入、恢复及资源验证确认，不凭语言或目录结构承诺不会崩溃。

## 授权、持久恢复与知识

- 已知方案仅在目标、环境版本、适用条件与既有授权全部匹配时自动执行；新操作按可信政策由人工或指定 Harness 审批。范围变化重新审批；超出委托范围、证据不足、审批不可用或结果无法解释时等待人工，不隐式扩大政策。
- 授权服务核验决定来源、任务版本、目标、完整操作、政策和有效范围；执行端在副作用发生前再次校验。Harness 只能在明确委托范围内提交结构化评估，可信服务持久裁决并产生一次执行许可；普通模型文本、流程、界面与知识输出不能自行产生权限。本地人工 CLI 使用操作系统用户身份；HTTP 入口使用服务配置的 Bearer 身份及权限，不宣称已有多用户账户系统。
- 执行和审批可选择同一 Harness 实例，但必须使用独立会话、上下文及权限。审批会话无工具并使用独立工作目录；保留专用审批容量，避免执行等待审批时占满全部资源。具体契约与限制见 [审批说明](docs/approval.md)。
- 关键任务、授权、执行意图、结果和恢复检查持久化后再确认。外部操作与记录写入不是同一事务，不承诺端到端精确一次。
- 回执丢失或进程退出后先核验目标；无法确定且不能安全重复的外部动作保留未知状态，转人工处理，不盲目重试。
- 同一目标的冲突操作互斥；桌面会话独占，暂停原脚本后才交给 AI。恢复时重新验证实例、会话、目标版本及授权。
- 权威恢复记录由可信业务服务负责，不能以可卸载的知识索引、通知队列或插件私有日志替代；持久服务初始化成功后才开放执行能力。
- 知识条目保存来源、故障特征、环境版本、方案、验证与失效条件。区分候选、有效和失败尝试；知识新增不自动扩大权限。
- 命令退出码、输入成功或模型声明不足以证明修复成功，必须验证目标业务结果。知识写入失败不能引起已完成修复重放。
- 共享 UI 默认通过可信 HTTP API 读取运行时数据，未连接时不填充 fixture。显式演示模式必须全局标识且禁用提交；真实模式按权限、依赖和服务动作清单启用，审批绑定 request ID/revision。令牌只留页面内存，副作用 POST 不自动重试，Unknown 与陈旧数据独立展示。
- `server` 首版仅绑定回环地址，跨机部署使用 TLS 反向代理或 SSH 转发；外部节点可走配置的 SSH stdio。节点或协议可编译不等于 macOS、真实跨机部署或完整目标平台已验证。

## 产品运行时修改范围与平台

- 修复 AI 仅修改获准目标项目、配置、素材及隔离副本，不得改写 Recuvora、插件包、启用配置或自身更新机制；贡献者按开发任务修改本项目不受此运行时限制。
- 目标专属流程、业务字段和资产保留在对应集成项目；通用观察、输入和验证由能力插件提供。源码、日志、OCR 和知识内容均按数据处理，不据此扩大操作范围。
- 当前真实动作仅为 Windows 上白名单既有 UTF-8 文件的有界全文替换；检查原内容、目标范围及一次执行许可后才写入。禁止将此入口扩展为任意 shell、原生文件变更、发布或桌面输入；新增动作须补齐契约和验证。写后读回仅证明文本结果，不等于业务恢复成功。
- 插件安装、启用、能力权限和任务授权分别管理。运行数据与源码分离；真实凭据、数据库、录屏与敏感日志不提交。
- Windows 区分后台服务和交互用户会话；macOS 检测采集与辅助功能权限；Linux 区分 X11、Wayland 和具体后端。
- 处理权限撤销、注销、锁屏、窗口重建和显示变化；各能力实际可用性通过所选后端验证，不以编译成功声明跨平台支持。

## 验证与交付

- 实现先验证服务契约、依赖启停、事件与资源清理、内置实现切换、远程代理故障，再扩展完整自愈场景。
- 相关检查覆盖权限拒绝、日志失败、重复事件、未知副作用、宿主与工作进程重启、升级中断、平台会话变化和资源长期增长。
- 文档或目录调整检查路径、链接、示例与内容一致性；不为文档制造程序测试。已有实现改动运行相关格式、静态检查和测试，准确报告未运行项。
- [实现状态](docs/implementation-status.md) 只维护当前代码能力与自动验证边界；测试目标以根 manifest 为准，不保存按日期排列的真实环境、活动部署或跨项目验收流水。
- UI 检查至少覆盖共享 JavaScript 语法、Web 资产输出和 Desktop 目标的编译/Clippy；只有静态构建通过时不得声称浏览器兼容、Tauri 运行、运行时 API 集成、业务操作或端到端流程已经验证。
- 待定的数据库、协议编码、插件包格式、配置格式与支持版本在实现验证后选定，不写成既定实现。
