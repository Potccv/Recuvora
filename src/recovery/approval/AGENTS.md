# 审批内部开发规范

继承[自愈业务规范](../AGENTS.md)。

- 公开审批契约维护在父层 `approval.rs`；内部模块仅向父模块开放必需接口。
- 日志恢复和实时操作必须调用同一状态迁移检查；不得分别实现授权规则。
- 状态迁移保持纯计算；存储模块在同步成功后才更新内存并确认操作。
- 路径校验保持原有拒绝链接、硬链接及源码内运行数据的边界。
- 测试使用项目集中测试目录及项目外临时目录，不在这里创建测试或生成物。
