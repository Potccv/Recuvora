# 故障存储内部规范

继承[自愈业务规范](../AGENTS.md)。职责见[README](README.md)。

- 检查点与该批信号必须在一条同步日志中提交，写入失败不改变内存和游标。
- 实时与回放使用同一迁移校验；任何损坏或末尾残片均拒绝恢复。
- 故障确认只记录关注，不能解除故障、改变政策或产生执行许可。
- 路径校验拒绝源码内运行数据、链接、Windows reparse point 与日志硬链接；不得弱化单写锁或写失败停用行为。
- 测试只放项目 tests/ 并使用外部隔离临时目录。
